# Partition table for the Seeed XIAO ESP32-S3 Plus (16 MB flash).
#
# The board definition in PlatformIO declares only 8 MB and would otherwise
# default to default_8MB.csv, leaving half the flash unused; platformio.ini
# overrides both the flash size and this table.
#
# Layout mirrors the framework's stock default_16MB.csv: two 6.5 MB OTA slots
# (the firmware currently uses ~1.3 MB) and a 3.5 MB LittleFS partition holding
# the web UI built from web/ (~90 KB) plus qlockconf.json.
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x640000,
app1,     app,  ota_1,   0x650000, 0x640000,
spiffs,   data, spiffs,  0xc90000, 0x360000,
coredump, data, coredump,0xFF0000, 0x10000,
